先看 README.md。

你现在如果还停在「为设备命名」：
- 不要用 ceshi123-(末尾减号不合法)
- 改成 CESHI123，或点「跳过」
- 用本地账户，不要开 BitLocker

一次性：
1. 虚拟机里拷入 guest，管理员运行「注册开机自动换软件身份.bat」，关机
2. 宿主机拷入 host，运行「1_拍干净快照.bat」

以后每次新实验，只在宿主机点：
    2_一键变成新设备并开机.bat

只改客户机注册表，成不了另一台设备。必须连 VirtualBox 的 UUID / 磁盘序列号 / MAC / 虚拟 TPM 一起换。
